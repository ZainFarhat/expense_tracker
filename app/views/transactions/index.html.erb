<p id="notice"><%= notice %></p>

<h1>Transactions</h1>

<table>
  <thead>
    <tr>
      <th>Transactionaccount</th>
      <th>Transactiontype</th>
      <th>Transactionamount</th>
      <th>Transactiondate</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @transactions.each do |transaction| %>
      <tr>
        <td><%= transaction.account.LastName, transaction.account.FirstName %></td>
        <td><%= transaction.TransactionAccount %></td>
        <td><%= transaction.TransactionType %></td>
        <td><%= transaction.TransactionAmount %></td>
        <td><%= transaction.TransactionDate %></td>
        <td><%= link_to 'Show account', [@account] %></td>
        <td><%= link_to 'Edit Transaction', edit_account_transaction_path(@account, transaction) %></td>
        <td><%= link_to 'Destroy Transaction', [@account, transaction], method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Transaction', new_account_transaction_path(@account) %>
