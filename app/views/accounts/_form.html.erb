<%= form_with(model: account) do |form| %>
  <% if account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
        <% account.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :AccountName %>
    <%= form.text_field :AccountName %>
  </div>

  <div class="field">
    <%= form.label :AccountNumber %>
    <%= form.number_field :AccountNumber %>
  </div>

  <div class="field">
    <%= form.label :AccountType %>
    <%= form.text_field :AccountType %>
  </div>

  <div class="field">
    <%= form.label :AccountBalance %>
    <%= form.text_field :AccountBalance %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
